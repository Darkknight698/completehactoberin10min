#include <stdio.h>
int main()
{
    char s[100], t[100], m n;
    
    printf("Enter first string: ");
    scanf("%s", s);
    printf("Enter second string: ");
    scanf("%s", t);
    // calculate the length of string s
    // and store it in m
    for(m = 0; s1[i] != '\0'; ++m);
    for(n = 0; s2[j] != '\0'; ++n, ++m)
    {
        s[i] = t[j];
    }
    s[i] = '\0';
    printf("After concatenation: %s", s);
    return 0;
    
}
