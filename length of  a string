#include <stdio.h>
#include<string.h> /* for string functions */
int main()
{
    char s[1000];
    int length;
    printf("Enter a string:");
    scanf("%s", s);
    length= strlen(s);
    printf("Length of string: %d", length);
    return 0;
}
