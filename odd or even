#include <stdio.h>
int main()
{
    int number;
    printf("Enter an integer: ");
    scanf("%d", &number);
    !(number&1)?  printf("%d is even ", number) : printf("%d is odd ", number);  //it will increase speed of your code 
    return 0;
}
