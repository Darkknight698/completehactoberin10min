ALGORITHM:
1.input a number from user
2.check remainder when that number is divided by two.
3. if remainder is 0 ,then number is even else odd.
4.output the respective statements
#include <stdio.h>
int main()
{
    int number;
    print("Enter an integer: ");
    scanf("%d", &number)
    if(number % 2 == 0)
        printf("%d is even ", number);
    else
        printf("%d is odd ", number);
    return 0;
}
