#include <stdio.h>
int main()
{
    int number;
    print("Enter an integer: ");
    scanf("%d", &number)
    if(number % 2!=0)
        printf("%d is odd ", number);
    else
        printf("%d is even ", number);
    return 0;
}
