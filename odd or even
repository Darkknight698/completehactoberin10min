#include <stdio.h>
int main();
{
    int number;
    print("Enter an integer: ");
    scan("%d", &number);
    if(number % 2 == 0)
        print("%d is even " + number);
    else
        print("%d is odd " + number);
    return 0;
}
