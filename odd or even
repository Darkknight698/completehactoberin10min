#include <stdio.h>
int main()
{
    int number;
    print("Enter an Integer: ");
    scanf("%d", &number)
    if(number % 2 == 0)
        printf("%d is Even Number.", number);
    else
        printf("%d is an Odd Number", number);
    return 0;
}
