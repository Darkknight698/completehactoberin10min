#include <stdio.h>
int main()
{
    char s[100], s2[100], i;
    print("Enter string s: ")
    scan("%s",s);
    for(i = 0; s[i] != '\0'; ++i)
    {
        s2[i] = s[i];
    }
    s2[i] = '\0';
    printf("String s2: %s", s2);
   
}
