#include <stdio.h>
int main()
{
    char sa[100], sb[100], i;
    print("Enter string s1: ")
    scan("%s",sa);
    for(i = 0; sa[i] != '\0'; ++i)
    {
        sb[i] = sa[i];
    }
    sb[i] = '\0';
    printf("String s2: %s", sb);
   
}
